// ─── קאנון העולם ───
// החלטות שנסגרו ולא נפתחות מחדש. הקובץ הזה קיים כדי שכלל שנקבע פעם
// אחת לא ייגזל שוב בעוד חודשיים משיחה אחרת.

// ═══ שומרי האבן ═══
//
// הפורטלים נבנו בשפה האדריכלית של שומרי האבן העתיקים. קראג הוא האחרון
// מהם.
//
// המשמעות למשחק: הילד מכיר את הסילואטה של הקשת תשעה מסעות לפני שהוא
// יודע מה היא אומרת. הוא לומד בלי לחשוב — קשת אבן + אור זהוב = מעבר —
// ואז במסע 10 עומד מול אותה גיאומטריה בדיוק, ולא נכנס דרכה. היא קמה.
//
// מה שאסור: שהפורטל *יהיה* קראג ישן. זה היה הופך כל קשת ליצור ומוחק
// מקראג את הייחוד. הקרבה היא בשפה, לא בזהות.

export const STRUCTURE = {
  PORTAL: 'PORTAL',        // מעבר. הילד נכנס דרכו.
  GUARDIAN: 'GUARDIAN',    // שומר אבן רדום. קראג. לא נכנסים דרכו לעולם.
}

export const GUARDIAN_STATE = {
  DORMANT: 'DORMANT',      // מסעות 1–9. נראה כמו חלק מהנוף.
  STIRRING: 'STIRRING',    // תחילת מסע 10. התפרים מתחילים להיפתח.
  AWAKE: 'AWAKE',          // קם. מכאן הוא יצור, לא מבנה.
}

// ── הדקדוק הוויזואלי ──
// אותה שפה אדריכלית, שתי affordances שאי אפשר לבלבל ביניהן. זה מה
// שמונע מילד לנסות להיכנס בכל קשת אבן שהוא רואה.
//
//   פורטל פעיל   — חלל הקשת מלא בממברנת אנרגיה, תנועה פנימה, חלקיקים,
//                   והביקון מסמן אותו כיציאה.
//   שומר רדום    — החלל הוא חלל. רואים דרכו את הרחוב. אין ממברנה, אין
//                   אנימציית הזמנה, והביקון שותק לגביו.
export function affordanceOf(node) {
  const isPortal = node?.kind === STRUCTURE.PORTAL
  const open = isPortal && node.open === true

  return {
    membrane: open,          // משטח האנרגיה בתוך הקשת
    particles: open,
    inwardPull: open,        // אנימציית "בוא לכאן"
    beaconMarksExit: open,   // האם הביקון מסמן אותו כמעבר
    enterable: open,
  }
}

// שער קשיח. גם אם מישהו יעביר שומר עם open:true בטעות — הוא לא ייפתח.
// שומר אינו מעבר, בשום מצב, גם כשהוא ער.
export function isEnterable(node) {
  if (!node || node.kind !== STRUCTURE.PORTAL) return false
  return node.open === true
}

// ── מסע 10 ──
// ההתעוררות היא ברחוב, ב-AR, בגובה מלא. זה ה-payoff של סולם הגדלים
// (25 ס"מ ← 220 ס"מ) והרגע היחיד בעולם 1 שבו עיגון אמיתי באמת נדרש.
// ההחלטה על שכבת ה-AR מתקבלת *אחרי* מבחן ה-WOW של נימי, לא לפניו.
export const KRAAG_AWAKENS = {
  where: 'STREET',
  mission: 'm10',
  heightM: 2.20,
  // בשכבת מצלמה+חיישנים אנחנו שולטים בפריים, ולכן אפשר להקים אותו
  // *לתוך* המסגרת במקום לבקש מילד לסגת ארבעה מטרים ברחוב.
  framing: 'RISE_INTO_FRAME',
}
